
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Banco de dados local (SQLite)
*.sqlite3

# Pasta de arquivos enviados por usuários (imagens de produtos, etc)
SID/media/
SID/static/

# Variáveis de ambiente (Onde as chaves REALMENTE devem ficar)
.env

# Logs
*.log

# Pastas de ambiente virtual
venv/
env/
.venv/

git rm -r --cached "*/__pycache__/*"
git rm -r --cached "*.pyc"

# Remove o .env do cache do Git
git rm --cached SID/.env

# Remove o banco de dados local do cache do Git
git rm --cached SID/db.sqlite3